{
  "name": "island-generation",
  "type": "module",
  "version": "0.1.0",
  "description": "",
  "main": "out/index.js",
  "scripts": {
    "start": "npx tsc && ts-node build/index.js",
    "test": "npx tsc && NODE_OPTIONS=--experimental-vm-modules npx jest --verbose ./out",
    "test:coverage": "npx tsc && NODE_OPTIONS=--experimental-vm-modules npx jest --coverage ./out"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}
