{
  "name": "island-generation",
  "type": "module",
  "version": "0.1.0",
  "description": "",
  "main": "out/index.js",
  "scripts": {
    "build": "rm -rf out && npx tsc",
    "start": "npm run build && node out/src/index.js",
    "test": "npm run build && NODE_OPTIONS=--experimental-vm-modules npx jest --verbose ./out",
    "test:coverage": "npm run build && NODE_OPTIONS=--experimental-vm-modules npx jest --coverage ./out"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8",
    "typescript": "^4.8.2"
  }
}
